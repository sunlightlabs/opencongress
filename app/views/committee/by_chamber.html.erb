<% @content_for_sortby = render :partial => 'sort_committees', :locals => { :sort => @sort } %>

<div id="committee_summary">
  <ul class="list">
      <%= render(:partial => 'committees_list',
                 :as => :committee,
                 :collection => @committees ) %>
  </ul>
</div>
