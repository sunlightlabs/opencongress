<section class="section">

    <h2 class="section__header">Masthead: Header</h2>

    <header class="masthead">
        <div class="masthead__header">
            <div class="masthead__brand">
                <a href="/">
                    <h1 class="sr-only">OpenCongress</h1>
                    <img class="logo" alt="OpenCongress Logo" src="<%= image_path('opencongress_logo.svg') %>">
                </a>
            </div>
            <div class="masthead__actions">
                <button class="button__signup">Sign Up</button>
                <button class="button__login">Log In</button>
            </div>
        </div>
    </header>

    <div class="codeblock">
        <div class="codeblock__example">
            <%= example_code do %>
                <header class="masthead">
                    <div class="masthead__header">
                        <div class="masthead__brand">
                            <a href="/">
                                <h1 class="sr-only">OpenCongress</h1>
                                <img class="logo" alt="OpenCongress Logo" src="<%= image_path('opencongress_logo.svg') %>">
                            </a>
                        </div>
                        <div class="masthead__actions">
                            <button class="button__signup">Sign Up</button>
                            <button class="button__login">Log In</button>
                        </div>
                    </div>
                </header>
            <% end %>
        </div>

        <div class="codeblock__notes">
            <h4>Notes</h4>
            <p>The masthead header consists of two parts: the brand logo, and header actions. It is to be always used in conjuction with the masthead navigation.</p>
        </div>

    </div>

</section>

<section class="section">

    <h2 class="section__header">Masthead: Navigation</h2>

    <div class="codeblock">
        <div class="codeblock__example">
            <%= example_code do %>

            <% end %>
        </div>

        <div class="codeblock__notes">
            <h4>Notes</h4>
            <p></p>
        </div>
    </div>

</section>

<section class="section">

    <h2 class="section__header">Footer</h2>

    <footer class="footer">
        <div class="container">
            OpenCongress is built by Sunlight Foundation and powered by open data. Learn more below.
            <br>
            <span class="glyphicon glyphicon-chevron-down"></span>
        </div>
    </footer>

    <div class="codeblock">
        <div class="codeblock__example">
            <%= example_code do %>
                <footer class="footer">
                    <div class="container">
                        OpenCongress is built by Sunlight Foundation and powered by open data. Learn more below.
                        <br>
                        <span class="glyphicon glyphicon-chevron-down"></span>
                    </div>
                </footer>
            <% end %>
        </div>

        <div class="codeblock__notes">
            <h4>Notes</h4>
            <p>The footer sits at the bottom of every page on OpenCongress.</p>
        </div>
    </div>

</section>