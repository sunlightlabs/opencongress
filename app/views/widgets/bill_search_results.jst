<% if (bill_search_results.length > 0) { %>
<ul id="searchResults">
  <% _.each(bill_search_results, function(billContainer) {
      var bill = billContainer.bill; %>
    <li><a class="billSearchResult" href="#" data-bill="<%= bill.ident %>"><strong><%= bill.typenumber %></strong> &mdash; <%= bill.any_title(:bare).substring(0, 120) + (bill.any_title(:bare).length > 120 ? "..." : '') %></a></li>
  <% }); %>
</div>
<% } else { %>
Sorry, no matching bills were found.
<% } %>
